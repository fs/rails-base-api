{
  "id": "file:/definitions.json#",

  "definitions": {
    "user": {
      "required": ["attributes"],
      "properties": {
        "attributes": {
          "required": ["email", "full_name"],
          "properties": {
            "email": { "type": "string" },
            "full_name": { "type": ["string", "null"] }
          }
        }
      }
    },

    "jwt_token": {
      "required": ["attributes"],
      "properties": {
        "attributes": {
          "required": ["token"],
          "properties": {
            "token": { "type": "string" }
          }
        }
      }
    }
  }
}
